그 Builder Chat 보면 취지가 그거야. 우리가 연동되어있는 MCP를 AI가 호출할때 사실 MCP를 호출할때 사용할 Argument를 AI가 정하는거지, 그 외에는 LLM이 직접 개입되는         
부분이 없어. 그점을 활용해서 반복적으로 MCP를 호출해서 모니터링 할 사항이 있으면 그걸 실제 LLM이 MCP를 호출하기 위해 사용할 json형태의 Argument를 미리 조립해서          
적용할 자산을 지정하고, 주기를 지정해서 돌리고, 임계치를 설정해서 Alert 여부를 설정하면 LLM을 나중에는 소모하지 않고도 반복해서 모니터링을 할 수 있지 않을까 하는게      
내 생각이야. 이를 위해 챗봇이 동작하는 방식을 좀더 고도화 해야할 것 같아. 우선 원래 MCP는 호출할때 Asset도 지정을 해야할지 모르겠으나, 우리는 Asset 대상은 UI로          
선택하기때문에 JSON에 포함될 필요는 없어. 그리고 어떤 MCP가 있는지 모르니까 내가 원하는 방식을 어떤 MCP들을 활용해야하는지 같이 의논하면서 정할 수 있으면 좋겠어.        
그리고 임계치 설정부분을 좀더 명확히 사용할 수 있게 조정해주고, 사용 예시(Placeholder)도 명확하게 몇가지 예시를 제대로 보여주면 좋을 것 같아. 이해하기 쉽게. 그리고      
채팅 UI에 모니터링 항목 선정, 임계치 설정이 있는데 그 왼쪽에 MCP목록 보는 탭을 하나 만들어서 현재 연동된 MCP 도구들 목록과 사용방법을 전부 한눈에 볼 수 있게 해주면      
좋을 것 같아. 그걸 보고 참고해서 원하는걸 구상하게. 마지막으로 임계치 설정 오른쪽 탭에서는 Action을 사용할지 여부에 따라 Action도 좌측 패널에 만들어 넣을 수 있게끔      
만들어주는 Phase를 추가하면 좋을 것 같아. 여기서도 내가 원하는 동작 (예를 들어 임계치를 초과한 프로세스 kill이라고 하면 SSH Exec MCP를 호출하면서 kill -9 {pid} 를       
담은 json 형태로 만들어줘서 그걸 내가 좌측 패널에 넣을 수 있게끔 말야. 그건 선택적으로 해서 안선택하면 진행 안하게. 선택해서 값 넣어두면 특정 임계치 도달 시 그          
Action 취하게 하면 좋을 것 같아. 이때 앞서 예의 {pid}처럼 뭔가 앞에 나온 출력 값의 특정 값을 넣어야 하는 경우 템플릿 형태로 넣는게 좋을지 어떻게 하면 좋을지             
모르겠지만 그런 것도 잘 넣을 수 있게 고민해서 적용해줘봐.